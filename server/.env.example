# 复制这个文件为 .env，然后填入你自己的 Key
# 去 https://console.anthropic.com 注册账号后创建 API Key
ANTHROPIC_API_KEY=your_api_key_here

# 服务器端口，本地跑不用改
PORT=3001

# 用于给登录令牌签名，随便填一串足够长且随机的字符串，不要外泄
JWT_SECRET=change_this_to_a_long_random_string
